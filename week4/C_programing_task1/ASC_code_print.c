#include <stdio.h>
    int main (void)
        {
            char b;
            printf("아스키코드 값을 입력하시오: ");
            scanf("%d", &b);
            printf("문자: %c입니다.", b);
            return 0;
        }