#include <stdio.h>
    int main (void)
        {
            int N;
            printf("어디까지 반복");
            scanf("%d", &N);

            for (int i =1; i<= N; i++) {
            printf("%d", i);

            }
            
            return 0;
        }